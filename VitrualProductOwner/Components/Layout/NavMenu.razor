<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">VitrualProductOwner</a>
        <div class="d-flex">
            <AuthorizeView Context="auth">
                <Authorized>
                    <span class="text-light me-3">Hello, @auth.User.Identity?.Name</span>
                    <form method="post" action="/logout" class="d-inline">
                        <AntiforgeryToken />
                        <button type="submit" class="btn btn-sm btn-outline-light">Logout</button>
                    </form>
                </Authorized>
                <NotAuthorized>
                    <a class="btn btn-sm btn-outline-light" href="/login">Login</a>
                </NotAuthorized>
            </AuthorizeView>
        </div>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler"/>

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>

        <AuthorizeView>
            <Authorized>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="stories">
                        <span class="bi bi-journal-text" aria-hidden="true"></span> Stories
                    </NavLink>
                </div>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="generate">
                        <span class="bi bi-lightbulb" aria-hidden="true"></span> Generate
                    </NavLink>
                </div>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="assets">
                        <span class="bi bi-folder2-open" aria-hidden="true"></span> Assets
                    </NavLink>
                </div>
            </Authorized>
        </AuthorizeView>
    </nav>
</div>
